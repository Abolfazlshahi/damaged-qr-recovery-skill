# `/audit`

Challenge a claimed recovery.

Check whether the report identifies observed bytes, RS-recovered bytes, external constraints, exact block tables, function-module exclusion, mask removal, source-module comparison, independent decoding, and uniqueness.

If any proof-critical step is missing, downgrade the claim rather than accepting a plausible payload.
