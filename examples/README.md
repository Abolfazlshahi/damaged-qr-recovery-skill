# Examples

Examples in this repository are deliberately synthetic or redacted.

- `agent-output.md` shows a safe final recovery report.
- `vendor-constraints.md` shows how domain-specific schemas should constrain, not replace, QR evidence.

Never add a real ticket, payment QR, live credential, or private session token as a public fixture.
