# Vendor Constraints

Vendor adapters may provide exact, documented constraints such as a prefix, fixed field length, alphabet, check digit, or printed-identifier equality.

They may only **filter** candidates. They must not silently mutate QR bytes or turn semantic plausibility into proof.
