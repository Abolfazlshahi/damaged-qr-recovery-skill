# Generic Agent Adapter

Load `skills/damaged-qr-recovery/SKILL.md` as the primary contract. Use `commands/` as task entry points and `references/` for deep technical stages.

Activate for damaged QR reconstruction, multi-image recovery, or validation of proposed QR reconstructions. Do not activate merely to generate an ordinary new QR.
