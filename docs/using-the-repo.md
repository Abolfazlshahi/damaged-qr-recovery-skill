# Using the Repository

The repository supports three modes.

## Skill-only

Copy the skill directory into an Agent Skills-compatible runtime.

## Agent + references

Load `SKILL.md` first, then the relevant reference files as deeper technical stages are reached.

## Developer

Install editable with `.[all]`, run `pytest`, and use the small CLI/scripts to inspect or validate real or synthetic images.
